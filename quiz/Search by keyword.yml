info:
  name: Search by keyword
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}?keyword=champion"
  params:
    - name: keyword
      value: champion
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
