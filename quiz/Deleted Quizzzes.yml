info:
  name: Deleted Quizzzes
  type: http
  seq: 1

http:
  method: DELETE
  url: "{{baseURL}}/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
