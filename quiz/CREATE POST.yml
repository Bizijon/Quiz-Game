info:
  name: CREATE POST
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseURL}}"
  body:
    type: json
    data: |-
      {
        "question": "NEW QUESTION",
        "answer": "new answer",
        "keywords" : ["mid lane catastrophe"]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
